# Function reference / 函式索引

Generated by `node scripts/document-functions.mjs`. Regenerate after changing named functions or comments; `--check` rejects drift. / 修改具名函式或註解後重新產生，本文件由檢查防止與程式不一致。

This index covers named function declarations and directly assigned arrow helpers in src, scripts and tests. Anonymous callbacks, getters and forwarding adapters are documented by their enclosing module and the [architecture reference](module-architecture.md). The scanner follows the repository's declaration formatting; it is not a full JavaScript parser. / 索引涵蓋具名函式與直接指派的箭頭 helper；匿名事件回呼、getter 和轉送介面由所屬模組說明，不另建功能。

## src/api/client.js

[Source / 原始碼](../src/api/client.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `keyFor` | Resolve one stable cache key regardless of query object property order. |
| `request` | Fetch or reuse a bounded response; failures remain retryable and never advance a page. |
| `invalidate` | Evict a structurally valid response that the domain store rejected, allowing a corrected retry. |
| `translate` | Re-fetch only previously requested resources, preserving lazy pages and isolating locale caches. |

## src/api/mock-transport.js

[Source / 原始碼](../src/api/mock-transport.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `clone` | Detach JSON fixtures and responses so consumers cannot mutate the transport database. |
| `create` | Create an isolated transport so tests can inject larger datasets, delays or retryable failures. |
| `fail` | Represent failures with the same fields a future HTTP adapter will expose. |
| `cursorFor` | Bind opaque cursors to both the dataset revision and the exact filtered collection. |
| `offsetFor` | Reject malformed, cross-resource and stale cursors before slicing a collection. |
| `paginate` | Return a bounded page with authoritative totals, including the empty and terminal cases. |
| `include` | Include category skill labels only; direct-text resources need no entity tables. |
| `emptyIncluded` | Direct-text resources require no entity tables or business translation catalogs. |
| `preview` | Attach a small relationship preview; the remaining skills have their own cursor endpoint. |
| `route` | Dispatch one documented resource request; no UI module reads fixtures directly. |
| `request` | Preserve asynchronous request semantics now so a future HTTP transport can be substituted. |

## src/app.js

[Source / 原始碼](../src/app.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `refresh` | Apply a single coordinated refresh for either a language change or a validated data replacement. |

## src/core/appearance-settings.js

[Source / 原始碼](../src/core/appearance-settings.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `normalize` | Accept only supported values; corrupted or older preferences cannot break startup. |
| `read` | Cookies are primary on HTTP(S); file:// and blocked storage degrade without errors. |
| `apply` | Apply before data initialization to avoid an unnecessary default-theme repaint. |
| `persist` | Save the same validated preferences to cookies and the offline fallback. |
| `update` | Notify independent UI and animation consumers through one validated state boundary. |

## src/core/data-contracts.js

[Source / 原始碼](../src/core/data-contracts.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `validateJourney` | Validate direct Journey fields without resolving organization, place or translation tables. |
| `validateExperiences` | Validate direct Experience cards without entity or translation-key lookup tables. |
| `validateProjects` | Validate Projects without renaming API fields or resolving legacy translation keys. |
| `validateSite` | Validate required fields and safe link/asset formats before committing site content. Empty social values are supported. Additional keys are not rejected or auto-rendered. This check does not resolve assets on disk or define the backend database schema. |
| `validate` | Validate identifiers, references and dates before replacing any visible content. |
| `reference` | Reject missing relations rather than silently rendering mismatched cards or markers. |
| `textKey` | Require every content reference to resolve in at least one loaded locale. |

## src/core/dates.js

[Source / 原始碼](../src/core/dates.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `careerMonthIndex` | Validate a YYYY-MM value and convert it to a comparable month index. |
| `careerMonthLabel` | Format a month with the active display locale in UTC. |
| `monthDuration` | Count inclusive calendar months; reject reversed ranges. |
| `validatePeriod` | Validate nullable end months and optional real calendar days before rendering dates. |
| `projectPeriodLabel` | Use one localized period format in cards and detail dialogs. |
| `careerDateLabels` | Format career dates, including expected completion and exact end days. |
| `workExperienceDuration` | Count the union of work months, excluding education and employment gaps. |

## src/core/i18n.js

[Source / 原始碼](../src/core/i18n.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `t` | Resolve a localized template with English fallback and named parameter interpolation. |
| `applyStatic` | Apply text and accessible attributes from the active catalog to static markup. |
| `setLanguage` | Persist a supported locale and notify the composition root exactly once. |

## src/core/icons.js

[Source / 原始碼](../src/core/icons.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `svg` | Render a trusted local icon by its registered Bootstrap name. |
| `mount` | Populate declarative icon placeholders in newly created markup. |

## src/core/map-geometry.js

[Source / 原始碼](../src/core/map-geometry.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `mercatorY` | Clamp only the projection near the poles; API validation retains the original coordinates. |
| `project` | Use the exact projection metadata of world.svg, independent of viewport scaling. |
| `route` | Derive a bounded flight arc from the endpoints, including coincident stops. |
| `layoutLabels` | Place measured labels in free space, prioritizing selection and hiding crowded labels. |
| `intersects` | Detect overlap between measured label rectangles during collision placement. |

## src/core/registry.js

[Source / 原始碼](../src/core/registry.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `register` | Register a feature factory; factories execute once after all scripts are loaded. |
| `get` | Resolve declared dependencies and reject circular module graphs. |
| `start` | Initialize once; subsequent calls return the same module instances. |
| `escape` | Escape all content inserted into HTML; SVG markup must come from the icon renderer. |
| `frameTask` | Coalesce repeated layout requests without losing the latest arguments. |
| `observeLayout` | Share resize and font invalidation between width-dependent components. |
| `disclosure` | Keep hidden state and the associated disclosure button synchronized. |
| `delegate` | Listen on a stable ancestor so re-rendering does not duplicate event handlers. |
| `svg` | Create an SVG node with explicit attributes in the correct namespace. |
| `t` | Translation shorthand used by feature modules. |
| `reducedMotion` | Read motion preferences at the time of interaction. |
| `isMobile` | Read the responsive mode from CSS so behavior and layout share one breakpoint. |

## src/core/store.js

[Source / 原始碼](../src/core/store.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `replaceJourney` | Replace only the active language journey; array order is authoritative and IDs stay numeric. |
| `emit` | Emit a narrow lifecycle event instead of forcing unrelated features to reinitialize. |
| `freeze` | Freeze nested records so views cannot accidentally mutate canonical data. |
| `replace` | Commit collection data only; validation failure preserves the snapshot and site cache. |
| `replaceNumbered` | Import a complete localized collection while retaining its API keys and array order. |
| `find` | Resolve category skill identities without exposing mutable records. |
| `skillLabel` | Look up a unique skill label; all components share this dictionary. |
| `mergeRows` | Merge category identities and skill prefixes without discarding already loaded labels. |
| `accept` | Validate a detached merge and its translation references before publishing it to consumers. |
| `definition` | Resolve resource routing centrally rather than teaching each component HTTP query formats. |
| `loadPage` | Append one page once; failures preserve the page number or cursor and loaded rows. |
| `initialize` | Bootstrap only the resources needed by the visible page; subsequent collection pages remain unloaded. |
| `prepareLocale` | Stage all requested-locale content before the language service commits the new UI locale. |
| `ownerRecord` | Resolve a category skill owner; card and dialog skills already arrive complete. |
| `skillsState` | Report complete card skills or a category relationship preview for the shared UI. |
| `loadSkills` | Fetch one additional relationship page and notify only skill presentations. |

## src/features/appearance.js

[Source / 原始碼](../src/features/appearance.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `refresh` | Refresh values without replacing focused controls during language or slider changes. |
| `close` | Close the settings panel and restore its trigger when dismissed by keyboard. |

## src/features/background-field.js

[Source / 原始碼](../src/features/background-field.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `stopped` | Suspend pulses for user pause, reduced motion or a hidden document. |
| `draw` | Use viewport-sized geometry so long documents never allocate an unbounded signal grid. |
| `pulse` | Two soft peaks preserve the reference heartbeat without flashing page content. |
| `schedule` | A small randomized batch runs only while visible, enabled and motion-safe. |
| `sync` | Motion preference wins over stored playback, without overwriting that preference. |
| `finishEntrance` | Never leave content hidden: entrance effects are finite and removed independently. |

## src/features/chatme.js

[Source / 原始碼](../src/features/chatme.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `cancelChatmeHide` | Cancel both idle and fade timers before changing disclosure state. |
| `chatmeEngaged` | Treat pointer hover and visible keyboard focus as active interaction. |
| `scheduleChatmeHide` | Start one inactivity countdown only when the disclosure is visible and idle. |
| `setChatme` | Apply open state, accessibility labels, focus restoration and timer policy. |
| `positionChatme` | Anchor beside the chat icon and clamp narrow-screen disclosures to the viewport. |
| `refresh` | Refresh UI labels and geometry after siteContent fills the chatme.* bindings. Content changes must preserve the existing open state and idle timers. |

## src/features/city-bubble.js

[Source / 原始碼](../src/features/city-bubble.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `position` | Constrain the tooltip and its tail to the map panel, leaving destination controls accessible. |
| `show` | Render direct Journey fields; category icons share the Experience presentation helper. |
| `hide` | Remove visual and accessibility state together when a tooltip closes. |
| `refresh` | Refresh an open tooltip after data or language updates without opening a closed tooltip. |

## src/features/collection-disclosure.js

[Source / 原始碼](../src/features/collection-disclosure.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `render` | Render trusted card markup while escaping all labels, identifiers and metadata. |
| `focusContent` | Prefer a useful control, falling back to the newly exposed content container. |

## src/features/journey.js

[Source / 原始碼](../src/features/journey.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `index` | Translate the selected ID into the current display order only when needed. |
| `highlight` | Synchronize the city marker with selection or an arriving plane. |
| `controls` | Update action labels without changing playback state. |
| `paint` | Render progress along the current SVG route; the last stop has no invented return flight. |
| `status` | Display the same selected record in the status panel and destination strip. |
| `select` | Select a known ID; pointer and keyboard choices explicitly pause automatic playback. |
| `build` | Build projected geometry from direct API fields without changing the supplied array order. |
| `resize` | Keep the world view fluid on ultrawide displays without moving geographic points. |
| `refresh` | Refresh localized records while retaining valid selected IDs and elapsed playback time. |
| `frame` | Advance only while visible; clamp resumed frames to avoid large playback jumps. |

## src/features/language-menu.js

[Source / 原始碼](../src/features/language-menu.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `refresh` | Synchronize radio state and the compact language badge. |
| `close` | Close the menu and optionally restore focus to its trigger. |
| `open` | Open the menu and focus the active language option. |

## src/features/mobile-menu.js

[Source / 原始碼](../src/features/mobile-menu.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `setOpen` | Apply visual and accessible state together. |

## src/features/navigation.js

[Source / 原始碼](../src/features/navigation.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `setActive` | Keep the active menu class and aria-current consistent. |
| `readHash` | Resolve decoded canonical and legacy URL fragments safely. |
| `writeHash` | Write explicit navigation to history; replace entries for passive scrolling. |
| `readingSection` | Choose the reading position, including the last section at document end. |
| `update` | Reconcile the active section with an ongoing explicit navigation. |
| `finishNavigation` | Release the temporary destination lock when navigation finishes or is interrupted. |
| `moveTo` | Scroll to a section while protecting its active state from intermediate sections. |
| `restoreHash` | Restore a valid deep link after load or browser history navigation. |
| `initializeNavigation` | Async content may finish after window.load; initialize deep links in either order. |

## src/features/pagination.js

[Source / 原始碼](../src/features/pagination.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `control` | Render the same loading, retry and terminal states for every paginated section. |
| `update` | Refresh status text without rebuilding cards while a request is in flight. |

## src/features/pointer-glow.js

[Source / 原始碼](../src/features/pointer-glow.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `hide` | Fade out on viewport exit, lost focus or a touch interaction. |
| `move` | Record only mouse coordinates; touch input hides the decorative layer. |
| `leave` | Ignore movement between page elements and hide only when leaving the viewport. |
| `syncPreference` | Attach tracking only while the device and motion preferences permit it. |

## src/features/projects.js

[Source / 原始碼](../src/features/projects.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `refresh` | Re-render the open project after language or data changes; removed records close cleanly. |

## src/features/render.js

[Source / 原始碼](../src/features/render.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `experience` | Render a career card with a stable identity shared by Journey and Experience. |
| `hasProjectDetail` | Empty detail objects do not create blank dialogs; strings remain plain text. |
| `projectCard` | Render a full project record without aliases for approved API fields. |
| `projectGroups` | Group consecutive equal months only, preserving authoritative server array order. |
| `projectDetail` | Hide absent detail sections and retain ordinary user-entered line breaks safely. |
| `section` | Render one nonempty detail field using its matching localized UI heading. |
| `journeyStops` | Render destination choices and derived labels from exactly the same ordered records. |
| `refresh` | Refresh the data-driven page while preserving collection disclosure state. |

## src/features/sidebar-rail.js

[Source / 原始碼](../src/features/sidebar-rail.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `compact` | Apply icon-only mode on desktop while leaving the mobile drawer independent. |
| `hideTooltip` | Remove only this component's description; links retain their localized accessible names. |
| `showTooltip` | Place one shared tooltip outside the scrollable rail so labels cannot be clipped. |
| `refresh` | Toggle one CSS-owned width token; page, background and chat observe the resulting layout. |

## src/features/site-content.js

[Source / 原始碼](../src/features/site-content.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `fullName` | Keep Latin names in given-family order; localized Han names use family-given order. |
| `refresh` | Fill direct SiteData bindings and derived labels after static UI catalog bindings. |
| `valueAt` | Attribute paths match the API keys exactly; text is never interpreted as HTML. |

## src/features/skill-layout.js

[Source / 原始碼](../src/features/skill-layout.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `skillsButtonText` | Share the Bootstrap plus-lg icon and localized count across every skill disclosure. |
| `skillsButtonLabel` | Provide a full screen-reader description for the compact expand control. |
| `tags` | Render skill chips for Experience, Projects, project dialogs and Skills categories. |
| `setSkillsExpanded` | Apply disclosure state and recompute the width-dependent layout. |
| `loadMore` | Request one bounded page; all views of this relationship share the data cache. |
| `layout` | Measure chip and toggle widths in the same CSS context before choosing a visible prefix. |
| `refresh` | Observe new groups and release detached groups before scheduling a shared layout pass. |
| `capture` | Capture stable owner IDs so reordering data does not lose expanded groups. |
| `restore` | Restore only groups that still exist after a content refresh. |

## src/features/stop-carousel.js

[Source / 原始碼](../src/features/stop-carousel.js)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `motion` | Choose scroll behavior from the current reduced-motion preference. |
| `updateArrows` | Disable only the arrow whose boundary has been reached. |
| `revealSelected` | Scroll the selected item into this strip without moving the outer page. |
| `refresh` | Measure overflow after layout and preserve pending selection-follow requests. |
| `slide` | Advance by a useful portion of the viewport with the current motion preference. |

## scripts/build.mjs

[Source / 原始碼](../scripts/build.mjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `within` | Reject absolute or escaping manifest paths before any filesystem operation. |
| `styles` | Inline authored CSS modules once; keep local font URLs relative to the output root. |
| `assets` | Copy only authored static assets; unknown files in dist are never deployed or deleted. |

## scripts/capture-readme.cjs

[Source / 原始碼](../scripts/capture-readme.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `main` | Use deterministic appearance preferences and real mock content for reproducible README previews. |

## scripts/document-functions.mjs

[Source / 原始碼](../scripts/document-functions.mjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `precedingComment` | Read an immediately preceding block or line comment, excluding unrelated earlier statements. |
| `functionInventory` | Index named declarations and directly assigned arrows; anonymous callbacks belong to their enclosing function. |
| `functionDocument` | Render repeatable Markdown so code comments and developer reference cannot drift silently. |

## scripts/lib/mock-data.mjs

[Source / 原始碼](../scripts/lib/mock-data.mjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `loadFrontend` | Load fixed frontend settings and trusted map geometry independently from API fixtures. |
| `read` | Read a frontend input only after verifying that its path stays inside src. |
| `loadMock` | Read only the mock files registered in config/build.json; missing files fail the build. Site, Journey, Experience and Projects locale files contain direct content; locales/{locale} contains translation keys for skills and categories only. Companion Markdown is never bundled. |

## scripts/package.mjs

[Source / 原始碼](../scripts/package.mjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `quote` | PowerShell literal quoting prevents path text from being evaluated as shell code. |

## scripts/serve.cjs

[Source / 原始碼](../scripts/serve.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `serve` | Resolve requests within the public directory and return explicit HTTP errors. |

## scripts/vendor-icons.mjs

[Source / 原始碼](../scripts/vendor-icons.mjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `download` | Fetch an upstream source asset and fail before accepting an HTTP error body. |
| `svg` | Render a trusted local icon by its registered Bootstrap name. |
| `mount` | Populate declarative icon placeholders in newly created markup. |

## tests/api-browser.cjs

[Source / 原始碼](../tests/api-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `inject` | Install a mock transport through the same interface a future HTTP adapter will implement. |
| `verifyStartup` | Check startup before and after window.load without depending on a successful site response. |
| `main` | Assert that increasing dataset size changes pages and totals without increasing initial DOM size. |

## tests/api.test.cjs

[Source / 原始碼](../tests/api.test.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `setup` | Install an observable transport while keeping tests independent from the embedded bundle. |

## tests/appearance-browser.cjs

[Source / 原始碼](../tests/appearance-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `main` | Verify theme settings, responsive controls, persistence and motion preferences. |
| `luminance` | Convert an RGB color to relative luminance for contrast assertions. |
| `cookie` | Find the persisted appearance cookie in the isolated browser context. |

## tests/boundaries-browser.cjs

[Source / 原始碼](../tests/boundaries-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `dataset` | Preserve locale identities while changing collection cardinalities independently of production data. |
| `main` | Assert bounded initial rendering, full page traversal, language persistence and layout limits. |

## tests/browser.cjs

[Source / 原始碼](../tests/browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `main` | Exercise the actual page using stable record IDs rather than positional test selectors. |

## tests/chat-idle-browser.cjs

[Source / 原始碼](../tests/chat-idle-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `fading` | Observe the active fade state without introducing fixed timing assumptions. |

## tests/data.test.cjs

[Source / 原始碼](../tests/data.test.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `copy` | Clone across VM boundaries to obtain a mutable candidate snapshot. |

## tests/experiences.test.cjs

[Source / 原始碼](../tests/experiences.test.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `copy` | Create a plain detached value for cross-context comparisons and mutations. |
| `expanded` | Keep identical record identities and order across the localized test pages. |

## tests/fixtures.cjs

[Source / 原始碼](../tests/fixtures.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `frontend` | Read fixed frontend inputs separately from mock API records. |
| `read` | Read a frontend fixture from the source tree. |
| `catalogs` | Build complete catalogs only for full-snapshot regression scenarios. |
| `fixture` | Return a detached dataset; each test can enlarge it without mutating files or another test. |
| `snapshot` | Build a complete normalized snapshot for import, policy and mutation tests. |

## tests/helpers.cjs

[Source / 原始碼](../tests/helpers.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `context` | Create a fresh service graph without a browser or network connection. |

## tests/interactions-browser.cjs

[Source / 原始碼](../tests/interactions-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `main` | Exercise visible outcomes and state transitions beyond the data-composition regression. |
| `pulse` | Inspect computed pulse parameters for icon animation assertions. |

## tests/journey-browser.cjs

[Source / 原始碼](../tests/journey-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `main` | Exercise behavior independently from the page's current number of career records. |
| `overlap` | Measure intersecting label rectangles to detect layout collisions. |

## tests/maintenance.test.cjs

[Source / 原始碼](../tests/maintenance.test.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `read` | Read maintained source text relative to the repository root. |
| `files` | List regular files with portable paths, excluding generated and vendor content. |
| `visit` | Walk the authored import graph rather than accepting files merely present on disk. |
| `featureIds` | Extract the canonical feature IDs before supplementary historical notes. |

## tests/projects.test.cjs

[Source / 原始碼](../tests/projects.test.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `copy` | Create a plain detached value for cross-context contract assertions. |

## tests/sidebar-rail-browser.cjs

[Source / 原始碼](../tests/sidebar-rail-browser.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `main` | Exercise desktop rail transitions, tooltips, themes and mobile isolation. |

## tests/spec.test.cjs

[Source / 原始碼](../tests/spec.test.cjs)

| Function / 函式 | Responsibility / 英文註解 |
| --- | --- |
| `resolve` | Resolve an internal OpenAPI JSON pointer without interpreting arbitrary remote references. |
